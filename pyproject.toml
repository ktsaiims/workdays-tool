[project]
name = "workdays-tool"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "holidays==0.93",
    "numpy>=2.4.4",
    "streamlit>=1.57.0",
]

[dependency-groups]
dev = [
    "pip-audit>=2.10.0",
]
